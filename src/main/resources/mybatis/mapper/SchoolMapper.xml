<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.college.mapper.SchoolMapper">
    <select id="findById" resultType="com.example.college.pojo.School">
         SELECT * FROM school WHERE sch_id=#{id}
    </select>
</mapper>